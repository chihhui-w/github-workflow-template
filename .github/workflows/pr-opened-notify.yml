name: Pull Request Opened Notification

on:
  pull_request:
    types:
      - opened
      - reopened
      - labeled

jobs:
  notify:
    runs-on: ubuntu-latest

    env:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      SLACK_USERS: ${{ secrets.SLACK_USERS }}

      PULL_REQUEST_NUMBER: ${{ github.event.pull_request.number }}
      PULL_REQUEST_TITLE: ${{ github.event.pull_request.title }}
      PULL_REQUEST_AUTHOR_NAME: ${{ github.event.pull_request.user.login }}
      PULL_REQUEST_AUTHOR_ICON_URL: ${{ github.event.pull_request.user.avatar_url }}
      PULL_REQUEST_URL: ${{ github.event.pull_request.html_url }}
      PULL_REQUEST_BODY: ${{ github.event.pull_request.body }}

      PULL_REQUEST_COMPARE_BRANCH_OWNER: ${{ github.event.pull_request.head.repo.owner.login }}
      PULL_REQUEST_COMPARE_BRANCH_NAME: ${{ github.event.pull_request.head.ref }}
      PULL_REQUEST_BASE_BRANCH_OWNER: ${{ github.event.pull_request.base.repo.owner.login }}
      PULL_REQUEST_BASE_BRANCH_NAME: ${{ github.event.pull_request.base.ref }}

      IS_PR_FROM_FORK: ${{ github.event.pull_request.head.repo.fork }}

    steps:
      - name: Resolve Slack Mentions
        uses: actions/github-script@v9
        with:
          script: |
            const slackUsers = JSON.parse(process.env.SLACK_USERS);
            const labels = context.payload.pull_request.labels.map(l => l.name);

            console.log({ labels });

            if (labels.length === 0) {
              core.setFailed(
                "This PR has no labels.\n\nPlease add at least one label and reopen the PR (or trigger the workflow again)."
              );
              return;
            }

            const mentions = labels
              .map(label => slackUsers[label])
              .filter(Boolean);

            console.log({ mentions });

            if (mentions.length > 0) {
              core.exportVariable(
                "SEND_USER_ID_MENTIONS",
                mentions.join(",")
              );
            } else {
              core.exportVariable("IS_SEND_HERE_MENTION", "true");
            }

            core.exportVariable("MAKE_PRETTY", "false");
            core.exportVariable("MAKE_COMPACT", "false");

      - name: Send Slack Notification
        uses: jun3453/slack-pr-open-notification-action@v1.3.0
